# 💱 Світ Валют

Сучасна платформа для обміну валют з онлайн-бронюванням курсу, адмін-панеллю та панеллю оператора.

![React](https://img.shields.io/badge/React-18.2-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.115-green)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4-cyan)
![Docker](https://img.shields.io/badge/Docker-Ready-blue)
![Render](https://img.shields.io/badge/Render-Ready-purple)

## 📋 Зміст

- [Можливості](#-можливості)
- [Технології](#-технології)
- [Швидкий старт](#-швидкий-старт)
- [Облікові записи](#-облікові-записи)
- [Структура проекту](#-структура-проекту)
- [API документація](#-api-документація)
- [Деплой](#-деплой)
- [Конфігурація](#-конфігурація)

## ✨ Можливості

### 🌐 Публічний сайт
- Калькулятор обміну валют в реальному часі
- Форма бронювання з вибором відділення та фіксацією курсу на 60 хвилин
- Інтерактивна карта з відділеннями (Leaflet)
- Пошук найближчого відділення за GPS/IP геолокацією
- Кнопки: дзвінок, чат, маршрут
- Адаптивний дизайн (mobile-first)
- Темна тема

### 👨‍💼 Адмін-панель (`/admin`)
- **Курси валют**: завантаження з Excel, перегляд базових/філіальних курсів
- **Бронювання**: перегляд та управління всіма заявками
- **Налаштування**:
  - Контактна інформація (телефони, email, соцмережі)
  - Управління відділеннями (адреси, координати, години роботи)
  - Увімкнення/вимкнення валют
  - FAQ
  - Послуги

### 👷 Панель оператора (`/operator`)
- Перегляд бронювань тільки свого відділення
- Підтвердження / завершення / скасування бронювань
- Завантаження актуальних курсів для свого відділення
- Автооновлення кожні 15 секунд
- Сповіщення про нові бронювання

## 🛠 Технології

### Frontend
- **React 18** + Vite
- **Tailwind CSS** - стилізація
- **React Router** - маршрутизація
- **Axios** - HTTP клієнт
- **Leaflet** - інтерактивні карти
- **Lucide Icons** - іконки
- **XLSX** - робота з Excel

### Backend
- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM
- **SQLite** - база даних
- **Pydantic** - валідація
- **Pandas + OpenPyXL** - обробка Excel

## 🚀 Швидкий старт

### Локальний запуск (рекомендовано для розробки)

```bash
# Клонування
git clone https://github.com/your-repo/svit-valut.git
cd svit-valut

# Запуск одним скриптом
chmod +x start.sh
./start.sh
```

Скрипт автоматично:
- Створить віртуальне середовище Python
- Встановить залежності
- Запустить backend на http://localhost:8000
- Запустить frontend на http://localhost:5173

### Docker (для продакшену)

```bash
# Збірка та запуск
docker-compose up -d

# Перегляд логів
docker-compose logs -f

# Зупинка
docker-compose down
```

**Доступ:**
- Frontend: http://localhost
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Ручний запуск

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload --port 8000

# Frontend (в іншому терміналі)
cd frontend
npm install
npm run dev
```

## 🔐 Облікові записи

| Роль | Логін | Пароль | Відділення |
|------|-------|--------|------------|
| Адміністратор | `admin` | `admin123` | Всі |
| Оператор 1 | `operator1` | `op1pass` | вул. Старовокзальна, 23 |
| Оператор 2 | `operator2` | `op2pass` | вул. В. Васильківська, 110 |
| Оператор 3 | `operator3` | `op3pass` | вул. В. Васильківська, 130 |
| Оператор 4 | `operator4` | `op4pass` | вул. Р. Окіпної, 2 |

## 📁 Структура проекту

```
svit-valut/
├── backend/
│   ├── main.py              # FastAPI додаток (API endpoints)
│   ├── models.py            # SQLAlchemy моделі
│   ├── database.py          # Налаштування БД
│   ├── requirements.txt     # Python залежності
│   └── Dockerfile           # Docker конфіг (production)
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненти
│   │   │   ├── Header.jsx
│   │   │   ├── HeroSection.jsx      # Калькулятор + бронювання
│   │   │   ├── BranchesSection.jsx  # Секція відділень
│   │   │   ├── BranchesMap.jsx      # Leaflet карта
│   │   │   ├── RatesSection.jsx     # Курси валют
│   │   │   ├── FAQSection.jsx
│   │   │   ├── ServicesSection.jsx
│   │   │   └── Footer.jsx
│   │   ├── pages/
│   │   │   ├── AdminDashboard.jsx   # Адмін-панель
│   │   │   ├── OperatorDashboard.jsx # Панель оператора
│   │   │   ├── SettingsPage.jsx     # Налаштування
│   │   │   ├── LoginPage.jsx
│   │   │   └── RatesPage.jsx
│   │   ├── services/
│   │   │   └── api.js       # API клієнт
│   │   └── App.jsx          # Головний компонент
│   ├── public/
│   │   ├── sitemap.xml
│   │   └── robots.txt
│   ├── package.json
│   ├── vite.config.js
│   ├── tailwind.config.js
│   └── Dockerfile           # Docker конфіг (nginx)
├── docker-compose.yml       # Docker Compose конфіг
├── render.yaml              # Render.com конфіг
├── start.sh                 # Скрипт локального запуску
├── .gitignore
└── README.md
```

## 📚 API документація

Swagger UI: http://localhost:8000/docs  
ReDoc: http://localhost:8000/redoc

### Публічні ендпоінти

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/health` | Перевірка статусу API |
| GET | `/api/currencies` | Список валют |
| GET | `/api/rates` | Поточні курси |
| GET | `/api/branches` | Список відділень |
| POST | `/api/reservations` | Створити бронювання |
| GET | `/api/settings` | Налаштування сайту |
| GET | `/api/faqs` | FAQ |
| GET | `/api/services` | Послуги |

### Адмін ендпоінти

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| POST | `/api/admin/login` | Авторизація |
| POST | `/api/admin/rates/upload` | Завантажити курси з Excel |
| GET | `/api/admin/reservations` | Всі бронювання |
| PUT | `/api/admin/reservations/{id}/assign` | Призначити оператору |
| PUT | `/api/admin/settings` | Оновити налаштування |
| PUT | `/api/admin/branches/{id}` | Оновити відділення |

### Оператор ендпоінти

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/operator/reservations` | Бронювання філії |
| POST | `/api/operator/reservations/{id}/confirm` | Підтвердити |
| POST | `/api/operator/reservations/{id}/complete` | Завершити |
| POST | `/api/operator/reservations/{id}/cancel` | Скасувати |
| GET | `/api/operator/rates/download` | Завантажити курси |

## 🌍 Деплой

### Render.com (рекомендовано)

1. Завантажте репозиторій на GitHub
2. Зайдіть на [render.com](https://render.com)
3. Створіть новий **Blueprint** з вашого репозиторію
4. Render автоматично використає `render.yaml`
5. Після деплою оновіть `VITE_API_URL` у frontend сервісі

**Важливо:** Після першого деплою backend отримає URL. Скопіюйте його та встановіть як `VITE_API_URL` для frontend.

### Docker (VPS/Cloud)

```bash
# На сервері
git clone https://github.com/your-repo/svit-valut.git
cd svit-valut

# Оновіть VITE_API_URL в docker-compose.yml
# Запустіть
docker-compose up -d
```

### Ручний деплой

**Backend (Render/Railway/Heroku):**
- Runtime: Python 3.11
- Build: `pip install -r requirements.txt`
- Start: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- Root: `backend`

**Frontend (Netlify/Vercel/Render Static):**
- Build: `npm install && npm run build`
- Publish: `dist`
- Root: `frontend`
- Env: `VITE_API_URL=https://your-backend-url`

## ⚙️ Конфігурація

### Змінні середовища

**Frontend (.env):**
```env
VITE_API_URL=http://localhost:8000
```

**Backend:**
```env
TZ=Europe/Kyiv
DATABASE_URL=sqlite:///./svit_valut.db
```

### Формат Excel для завантаження курсів

Файл повинен містити аркуш з такими колонками:

| Валюта | Купівля | Продаж |
|--------|---------|--------|
| USD | 42.10 | 42.15 |
| EUR | 49.30 | 49.35 |

Або за форматом файлу "Світ Валют.xlsx" з матричним розташуванням.

## 📱 Маршрути

| Шлях | Опис |
|------|------|
| `/` | Головна сторінка |
| `/rates` | Всі курси валют |
| `/services` | Всі послуги |
| `/services/:slug` | Сторінка послуги |
| `/login` | Авторизація |
| `/admin` | Адмін-панель |
| `/admin/settings` | Налаштування |
| `/operator` | Панель оператора |

## 🔧 Розробка

```bash
# Запуск в режимі розробки
./start.sh

# Збірка frontend для продакшену
cd frontend && npm run build

# Перевірка синтаксису backend
cd backend && python -m py_compile main.py
```

## 📄 Ліцензія

MIT

---

Розроблено з ❤️ для Світ Валют
